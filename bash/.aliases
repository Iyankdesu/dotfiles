#!/bin/sh

# cd
alias ..="cd .."
alias ...="../.."
alias ....="../../.."

# general
alias l="clear -x"
alias x="exit"
alias q="exit"
alias bat="bat --no-pager --style plain"
alias src="source"
alias mkdir="mkdir -p -v"
alias cp="cp -v"
alias mv="mv -v"

alias vim="nvim"

# yt-dlp
alias yt="yt-dlp -f 22"

# dir
alias cdc="cd /mnt/Iyank/Code"
alias cdk="cd /mnt/Iyank/Kuliah"

# exa
alias ls="exa --icons --sort type"
alias la="exa --icons --sort type --all"
alias lc="echo Total files: $(exa | wc -l)"
alias ll="exa --icons --sort type --long"
alias lt="exa --icons --tree --sort type"

# trash-cli
alias rm="trash-put"

# code stuff
alias code="code --new-window"
alias chrome="google-chrome-stable"

# sudo
alias sudo="doas"

# apt
alias install="sudo apt install"
alias remove="sudo apt remove"
alias autoremove="sudo apt autoremove"
alias search="sudo apt search"
alias update="sudo apt update"
alias upgrade="sudo apt upgrade"
alias query="sudo apt list --installed"
alias purge="sudo apt remove --purge"
alias only-upgrade="sudo apt install --only-upgrade"

# git
alias gc="git clone"
alias ga="git add"
alias gt="git status"
alias gm="git commit -m"
alias gp="git push"
alias gu="git pull"
alias gd="git diff"
alias gk="git checkout"
alias gl="git log --graph --decorate --oneline"

# network
alias ping="ping -c 4"

# programming lang
alias rb="ruby"

# python
alias py="python3"
alias workon="source .venv/bin/activate"
alias workoff="deactivate"

# translate
alias tl="trans -brief -target"
